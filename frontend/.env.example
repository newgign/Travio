VITE_API_URL=http://localhost:5000/api

VITE_SUPPORT_PHONE="+7 707 630 61 35"
VITE_SUPPORT_EMAIL="newgign@gmail.com"
# Render: set VITE_API_URL to the backend HTTPS origin plus /api before building.
# VITE_* variables are public and embedded at build time; never place secrets here.

# Public display opt-in, requires server-authorized TEST offers; never enables booking.
VITE_HOTELBEDS_STAGING_TEST_ENABLED=false
